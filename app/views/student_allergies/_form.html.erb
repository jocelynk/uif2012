<%= form_for(@student_allergy) do |f| %>
  <% if @student_allergy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_allergy.errors.count, "error") %> prohibited this student_allergy from being saved:</h2>

      <ul>
      <% @student_allergy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br />
    <%= f.number_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :allergy_id %><br />
    <%= f.number_field :allergy_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
