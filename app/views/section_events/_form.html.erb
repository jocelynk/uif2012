<%= simple_form_for(@section_event) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :section_id %>
    <%= f.input :event_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
