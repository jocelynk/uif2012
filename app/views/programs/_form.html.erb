<%= simple_form_for @program, :html => { :class => 'form-horizontal' } do |f| %>
  
  <%= f.input :name, :placeholder => 'Name' %>
  <%= f.input :description, :placeholder => 'Enter description' %>
  <%= f.association :department, :prompt => 'Choose'%>
  <%= f.input :min_grade, :collection =>1..13, :prompt => 'Choose' %>
  <%= f.input :max_grade, :collection =>1..13, :prompt => 'Choose' %>
  <%= f.input :max_capacity %>
  <%= f.input :start_date, :order => [:month, :day, :year] %>
  <%= f.input :end_date, :order => [:month, :day, :year] %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                programs_path, :class => 'btn' %>
  </div>
<% end %>