<div class="field">
   <%= f.label :section , class: "section"%><br />
   <%= f.grouped_collection_select :section_id, Program.all, :sections, :name, :id, :name, :prompt => "Choose a Section" %>
  </div>
  <%= f.link_to_remove "Remove this registration" %>
